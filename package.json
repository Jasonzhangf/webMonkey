{
  "name": "web-automation-orchestrator-test",
  "version": "1.0.0",
  "description": "Test server for Web Automation Orchestrator browser extension",
  "main": "test-server.js",
  "scripts": {
    "start": "node test-server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "ws": "^8.13.0"
  }
}